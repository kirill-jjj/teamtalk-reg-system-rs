pre-commit:
  parallel: false
  commands:
    fmt:
      glob: "*.rs"
      run: "cargo fmt --all --"
    clippy:
      glob: "*.rs"
      run: "cargo clippy --all-targets --all-features -- -D warnings"
    sqlx-prepare:
      glob: "*.rs"
      run: "cargo sqlx prepare"
